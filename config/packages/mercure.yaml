mercure:
    hubs:
        default:
            url: '%env(CADDY_MERCURE_URL)%'
            public_url: '%env(CADDY_MERCURE_PUBLIC_URL)%'
            jwt:
                secret: '%env(CADDY_MERCURE_JWT_SECRET)%'
                publish: '*'
